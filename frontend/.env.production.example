# ── Vercel Frontend Environment Variables ─────────────────────────────────────
# Set these in Vercel Dashboard → Project → Settings → Environment Variables
# (or use the Vercel CLI: vercel env add VITE_API_URL)

# Core service URL on Render (handles most API routes)
VITE_API_URL=https://polaris-core.onrender.com/api/v1

# WebSocket base URL — points to the notif/core service on Render
VITE_WS_URL=wss://polaris-core.onrender.com

# Stripe publishable key (safe to expose in frontend)
VITE_STRIPE_PK=pk_test_YOUR_PUBLISHABLE_KEY_HERE
