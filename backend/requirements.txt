# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database (async)
sqlalchemy>=2.0.30
asyncpg>=0.30.0
alembic>=1.13.1

# Settings & validation
pydantic-settings>=2.2.1
pydantic[email]>=2.7.1

# Auth & Security
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1

# 2FA / TOTP
pyotp>=2.9.0
Pillow>=10.3.0

# QR code generation (Python 3.13-compatible)
qrcode>=8.0

# Rate limiting
slowapi>=0.1.9

# AWS S3
boto3>=1.34.99

# Payments
stripe>=9.5.0

# PDF (certificates)
reportlab>=4.1.0

# Multipart (file uploads)
python-multipart>=0.0.9

# Email
aiosmtplib>=3.0.0

# Redis (Q&A WebSocket fan-out, optional rate-limit backend)
redis>=5.0.0

# Tests
pytest>=8.2.0
pytest-asyncio>=0.23.6
httpx>=0.27.0
aiosqlite>=0.20.0
